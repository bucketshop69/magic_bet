{
  "license": "ISC",
  "scripts": {
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
    "crank:install": "yarn --cwd services/crank install",
    "crank:dev": "yarn --cwd services/crank dev",
    "crank:build": "yarn --cwd services/crank build",
    "crank:start": "yarn --cwd services/crank start",
    "crank:ws-smoke": "yarn --cwd services/crank ws:smoke",
    "web:install": "yarn --cwd apps/web install",
    "web:dev": "yarn --cwd apps/web dev",
    "web:build": "yarn --cwd apps/web build",
    "web:preview": "yarn --cwd apps/web preview"
  },
  "dependencies": {
    "@coral-xyz/anchor": "0.32.1",
    "@magicblock-labs/ephemeral-rollups-sdk": "^0.8.5"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
